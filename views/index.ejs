<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>To Do List</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&family=Sono:wght@300;500&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>

<body>
  <!-- <section class="container"> -->
  <h1>To Do List</h1>

  <form id= "todo-form">
    <input type="text" placeholder="What you gotta do?" name="things" id="todo-input">
    <input type="date" placeholder="When is it Due?" name="dueDate" id="todo-date">
    <button>+</button>
  </form>
  <ul id= "todo-list"class="list">
    <% for(let i=0; i<list.length; i++) {%>
      <li class="message">
       
        <span>
          <%= list[i].things %>
        </span>
        <span>
          <%= list[i].dueDate %>
        </span>
        
        <button class="checked">✔</button> 
      </li>
      <% } %>
  </ul>
  <button class="exec" id="borra">Clear</button>
  <!-- </section> -->
  
  <script src="main.js"></script>

</body>

</html>